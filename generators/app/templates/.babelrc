{
  "presets": [
    [
      "@babel/env",
      {
        "shippedProposals": true,
        "targets": {
          "node": "12.14"
        },
        "modules": "cjs"
      }
    ]
  ]

  // Optional Plugins -> Proposals. Property does have to be used. Plugins have to be installed via npm.
  // "plugins": [
  // ["@babel/plugin-proposal-pipeline-operator", { "proposal": "minimal" }],
  // ]
}
